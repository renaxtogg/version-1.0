{
  "name": "mythos-saas",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "vite build && vite build --config vite.config.delivery-cliente.mjs && vite build --config vite.config.cocina.mjs && vite build --config vite.config.index.mjs && vite build --config vite.config.mozo.mjs && vite build --config vite.config.caja.mjs && vite build --config vite.config.gerente.mjs && vite build --config vite.config.admin.mjs && vite build --config vite.config.superadmin.mjs && vite build --config vite.config.proveedor.mjs && vite build --config vite.config.clientes.mjs && vite build --config vite.config.riders.mjs",
    "dev": "vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "@upstash/ratelimit": "^2.0.8",
    "@upstash/redis": "^1.38.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}

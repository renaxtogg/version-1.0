# CREDENCIALES Y DATOS SENSIBLES — nunca subir a GitHub
public/config.js
# Artefactos del simulacro (contienen PAT, contraseñas y SQL de datos de prueba)
_simulacion/
.env
.env.local
.env.production
.env.*.local

# Dependencias
node_modules/

# Output de build Vite (PR-11) — se regenera con `npm run build`, no versionar
public/build/

# Archivos temporales del proceso de extracción del diseño
design.gz
Mesa_App.html
design_extracted/
supabase/.temp/

# Vercel
.vercel
.env.vercel

# OS y editor
.DS_Store
Thumbs.db
.vscode/settings.json
*.code-workspace

# Respaldos de migraciones y helpers de copy-paste a Supabase (no versionar)
supabase/migrations/*.bak
EJECUTAR_EN_SUPABASE.sql

# pdf.js vendorizado — lo regenera scripts/vendor-pdfjs.mjs en cada build
public/vendor/
